# Repos that vendor lib/ via git subtree. `make fanout` walks this list.
# One path per line; ~ is expanded.
#
# This list is the intended set, not yet the actual one. Only tmux-agent-mesh has
# a vendored lib/ today; the other four are D-2b. `make fanout` prints
# "skip (lib/ not vendored yet)" for those rather than failing, so the list can
# describe the target without breaking the target that walks it. Run
# `tmux-toolkit consumers` for what is actually vendored and at which version.
~/Code/tmux-agent-tracker
~/Code/tmux-agent-resumer
~/Code/tmux-agent-mesh
~/Code/tmux-worktree
~/Code/tmux-session-order
